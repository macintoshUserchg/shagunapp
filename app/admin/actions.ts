// Minimal actions file for static site build
export const loginAction = async () => {
  return { success: false, error: "Login disabled in static site" };
};

export const logoutAction = async () => {
  return { success: false, error: "Logout disabled in static site" };
};

export const saveCategoryAction = async () => {
  return { success: false, error: "Saving disabled in static site" };
};

export const deleteCategoryAction = async () => {
  return { success: false, error: "Deletion disabled in static site" };
};

export const saveProductAction = async () => {
  return { success: false, error: "Saving disabled in static site" };
};

export const deleteProductAction = async () => {
  return { success: false, error: "Deletion disabled in static site" };
};

export const saveStoreLocationAction = async () => {
  return { success: false, error: "Saving disabled in static site" };
};

export const deleteStoreLocationAction = async () => {
  return { success: false, error: "Deletion disabled in static site" };
};

export const updateSettingsAction = async () => {
  return { success: false, error: "Saving disabled in static site" };
};